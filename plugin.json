{
  "name": "DbStress",
  "prefix": "DbStress",
  "version": "0.1.0",
  "enabled": true,
  "description": "数据库压测插件，覆盖 CRUD 场景，支持 MySQL / PostgreSQL / MongoDB。通过 executeRawDatabaseCommand 发起压测，输出行业标准延迟与 TPS 报告。",
  "entities": [],
  "components": {
    "base": {
      "scriptPaths": ["base"],
      "entry": "plugin_entry"
    },
    "cell": {
      "scriptPaths": ["cell"],
      "entry": "plugin_entry"
    }
  }
}
